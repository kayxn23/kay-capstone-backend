CREATE TABLE game(
    id BIGINT PRIMARY KEY     NOT NULL,
    organizer VARCHAR(20),
    location VARCHAR(20)
);

1. create database capstonebackend
2. \connect capstonebackend
3. CREATE TABLE game(
       id BIGINT PRIMARY KEY     NOT NULL,
       organizer VARCHAR(20),
       location VARCHAR(20)
   );
4. \dt
5. select * from game;    (read rows )